#include <stdio.h>
#include <stdlib.h>
//file .c, .m => compile + implementation 
int sum(int x, int y) {
	return x + y;
}
int substract(int x, int y) {
	return x - y;
}
void doNothing() {
	printf("I do nothing");
}

